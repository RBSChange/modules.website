Version: 3.5.0

Mise à jour fichier de config block.xml
Ajout des attributs :
	- section -> top = 1er section, si omi utilisation du module
    - dropModels -> type utilisé lors de l'insertion d'un document

	- inject -> type à injecter
	- templateModule -> module à utilisé lors de l'injection pour trouver les templates
	- requestModule  -> module à utilisé pour les parametres d'entrée.

Attributs existant : type*, hidden, icon, label, dashboard, afterAll, editable, beforeAll